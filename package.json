{
    "name": "klassroom-root",
    "description": "Service to create optimized seating charts.",
    "version": "1.0.0",
    "private": true,
    "dependencies": {
        "aws-sdk": "^2.799.0"
    },
    "devDependencies": {
        "jest": "^26.6.3"
    },
    "scripts": {
        "test": "jest",
        "build-all": "npm i && cd lambdas/klassroom && npm i"
    }
}
